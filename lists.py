#Multiple values in a single variable
#Different data types can be put into lists but its advised not to
tvcharacters = ["Wednesday", "Enid", "Steve"]
print(len(tvcharacters)) #Prints the length of the list
tvcharacters.append("Eleven") #Adds a new element on the end
print(tvcharacters)
tvcharacters.remove("Steve") #Removes an elements from the list